# 复制为 .env 后填写。真实密钥绝不提交到仓库（README R8）。

# LLM 供应商（OpenAI 兼容接口，M2 起需要）
LLM_PROVIDER=deepseek
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=
LLM_MODEL=deepseek-chat

# 本地嵌入模型（M2 动态主题发现用）
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
