# M1（当前阶段）：纯标准库实现，零第三方依赖，保证本地可直接运行。
# 后续里程碑按需启用以下依赖（对应 PLAN.md 技术选型）：
#
# M2 分析层：
#   httpx>=0.27
#   polars>=1.0
#   sentence-transformers>=3.0
#   scikit-learn>=1.5
#
# M4 应用层：
#   当前 Web UI（app/server.py）为纯标准库实现，无需安装即可运行。
#   fastapi>=0.115
#   uvicorn>=0.30
#   pydantic>=2.8
#   （如需 React 前端，另行引入 node 工具链）
#
# 测试：
#   pytest>=8
